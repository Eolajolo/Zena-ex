const auth = require('./auth');
const account = require('./account');
const wallet = require('./wallet');
const payment = require('./payment');
const eTrades = require('./e-trades');
const giftcard = require('./giftcard');

module.exports = {
  auth,
  account,
  wallet,
  payment,
  eTrades,
  giftcard
};
